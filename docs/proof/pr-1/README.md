# Wi-Fi validation evidence

Candidate `ccfe416295929c804dd27c9a8f5b9413f184124b`. This evidence contains generated UI samples and sanitized scanner events only. Personal page images and PDFs are retained locally and excluded.

The hardware capture used a production-backend harness. The later normal-window check correctly timed out after the scanner disappeared from the network; the last loaded sheet was not consumed.
