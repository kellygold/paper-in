#!/bin/bash
# Shared source groups, relative to the repository root. Keep build/test wiring here.
paper_documents=(app/support/PaperError.swift app/filing/FilingTypes.swift app/documents/*.swift)
paper_application=(app/support/*.swift app/documents/*.swift app/scanning/*.swift app/ui/*.swift app/filing/*.swift)
